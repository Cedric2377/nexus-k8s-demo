# Persistent Volume Claim to mount NFS
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: nexus-nfs-pvc
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 5Gi

